/// <reference types="node" />
import { Logger } from '@stryker-mutator/api/logging';
import { Config } from '@jest/types';
import JestConfigLoader from './JestConfigLoader';
export default class ReactScriptsTSJestConfigLoader implements JestConfigLoader {
    private readonly log;
    private readonly resolve;
    private readonly projectRoot;
    static inject: ["logger", "resolve", "projectRoot"];
    constructor(log: Logger, resolve: RequireResolve, projectRoot: string);
    loadConfig(): Config.InitialOptions;
    private isNodeErrnoException;
    private createJestConfig;
}
//# sourceMappingURL=ReactScriptsTSJestConfigLoader.d.ts.map