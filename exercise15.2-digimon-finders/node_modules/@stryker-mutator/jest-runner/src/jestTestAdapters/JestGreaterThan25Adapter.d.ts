import { Logger } from '@stryker-mutator/api/logging';
import { Config } from '@jest/types';
import { JestRunResult } from '../JestRunResult';
import JestTestAdapter from './JestTestAdapter';
export default class JestGreaterThan25Adapter implements JestTestAdapter {
    private readonly log;
    static inject: ["logger"];
    constructor(log: Logger);
    run(jestConfig: Config.InitialOptions, projectRoot: string, fileNameUnderTest?: string): Promise<JestRunResult>;
}
//# sourceMappingURL=JestGreaterThan25Adapter.d.ts.map