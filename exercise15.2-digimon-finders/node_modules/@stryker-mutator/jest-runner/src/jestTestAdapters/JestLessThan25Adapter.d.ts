import { Logger } from '@stryker-mutator/api/logging';
import { JestRunResult } from '../JestRunResult';
import JestTestAdapter from './JestTestAdapter';
/**
 * The adapter used for 22 < Jest < 25.
 * It has a lot of `any` typings here, since the installed typings are not in sync.
 */
export default class JestLessThan25TestAdapter implements JestTestAdapter {
    private readonly log;
    static inject: ["logger"];
    constructor(log: Logger);
    run(jestConfig: Record<string, unknown>, projectRoot: string, fileNameUnderTest?: string): Promise<JestRunResult>;
}
//# sourceMappingURL=JestLessThan25Adapter.d.ts.map